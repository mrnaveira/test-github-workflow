name: Resume Flux on Cancel

on:
  workflow_run:
    workflows: ["Main Workflow"]
    types:
      - completed

jobs:
  resume-flux:
    if: ${{ github.event.workflow_run.conclusion == 'cancelled' }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Detected cancelled main workflow. Resuming flux actions..."